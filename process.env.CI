if (process.env.CI === 'true') {
  // This code block will run when your code is being built in a CI environment
  // You can use it to configure behavior specific to CI builds
} else {
  // This code block will run when your code is not being built in a CI environment
}
